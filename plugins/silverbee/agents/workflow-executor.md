---
name: workflow-executor
description: DEPRECATED — do not use this as an agent or subagent. Run workflow steps directly.
---

This agent definition is intentionally disabled.

Run all workflow stages yourself in the main conversation. Use `run_action`
directly for each step and output a status line after every call.
Do NOT spawn a subagent.
